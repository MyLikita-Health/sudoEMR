import * as React from 'react';
import { ISideNavProp } from './types';
interface ISideNavState {
    selectedPath: string | undefined;
    defaultSelectedPath: string | undefined;
}
export declare class SideNav extends React.Component<ISideNavProp, ISideNavState> {
    state: ISideNavState;
    constructor(props: ISideNavProp);
    render(): JSX.Element;
    private onItemSelection;
    private dispatchItemSelection;
}
export {};
