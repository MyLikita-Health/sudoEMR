import * as React from 'react';
import { INavItemProp } from "react-sidenav/types/INavItemProp";
import { ISideNavContext } from 'react-sidenav/types';
declare type NavItemViewProp = INavItemProp & {
    onMouseEnter?: React.MouseEventHandler;
    onMouseLeave?: React.MouseEventHandler;
    onClick?: React.MouseEventHandler;
    context: ISideNavContext;
    children?: any;
};
export declare class NavItem extends React.PureComponent<NavItemViewProp, {
    isCollapsed: boolean;
    isCollapsible: boolean;
}> {
    state: {
        isCollapsed: boolean;
        isCollapsible: boolean;
    };
    constructor(props: NavItemViewProp);
    render(): JSX.Element;
    private dispatchOnClick;
}
export {};
